import React from 'react'
import UsaTemplate from '../USABANKS/UsaBanksTemplate'

const Wirechecks = () => {
    const text = "Wire Checks"
    const header = "company fullz & checks"
  return (
    <UsaTemplate text={text} header= {header} />
  )
}

export default Wirechecks